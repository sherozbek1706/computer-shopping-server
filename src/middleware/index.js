const hasRole = require("./hasRole");
const isLoggin = require("./isLoggin");
const uploadUsers = require("./uploadUsers");
const uploadComputer = require("./uploadComputers");

module.exports = {
  hasRole,
  isLoggin,
  uploadUsers,
  uploadComputer,
};
